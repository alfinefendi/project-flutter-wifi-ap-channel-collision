name: flutter_wifi_collision_detection
description: "a Flutter project for detecting WiFi collisions."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  wifi_scan: ^0.4.1+2
  fl_chart: ^1.1.1
  provider: ^6.1.5+1
  uuid: ^4.5.3
  shadcn_ui: ^0.46.2
  sqflite: ^2.4.2
  go_router: ^17.1.0
  flutter_lucide: ^1.8.2
  flutter_native_splash: ^2.4.7
  network_info_plus: ^7.0.0
  cristalyse: ^1.17.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
